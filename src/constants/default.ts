// Font-related types and constants
export type FontWeight = 200 | 300 | 400 | 500 | 600 | 700 | 800;
export type FontStyle = 'normal' | 'italic';

// Typography defaults
export const DEFAULT_FONT_SIZE = 16;
export const DEFAULT_FONT_WEIGHT: FontWeight = 400;
export const DEFAULT_FONT_STYLE: FontStyle = 'normal';

// TextInput specific defaults
export const DEFAULT_INPUT_FONT_SIZE = 14;
export const DEFAULT_PADDING_VERTICAL = 6;

// UI component defaults
export const DEFAULT_PLACEHOLDER = '';
export const DEFAULT_REQUIRED = false;
